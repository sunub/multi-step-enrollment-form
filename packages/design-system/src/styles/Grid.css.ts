import { style } from "@vanilla-extract/css";

export const gridCenter = style({
	display: "grid",
	placeItems: "center",
});

export const gridFlexStart = style({
	display: "grid",
	justifyItems: "start",
});
